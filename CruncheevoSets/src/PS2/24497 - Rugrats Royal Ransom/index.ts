/*import { AchievementSet, define as $ } from '@cruncheevos/core'

import makeAchievements from './achievements.ts'
import makeLeaderboards from './leaderboards.ts'
import makeRp from './rp.ts'

const set = new AchievementSet({
    gameId: 24497,
    title: 'Rugrats: Royal Ransom'
})


makeAchievements(set)
makeLeaderboards(set)
export const rich = makeRp()

export default set */